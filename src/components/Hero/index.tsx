import React from "react";
import { HeroSection, HeroTitle } from "./styles";

const Hero = () => {
    return (
    <HeroSection>
        <HeroTitle>Bem-vindo ao Meu Portfólio</HeroTitle>
    </HeroSection>
);
};

export default Hero;